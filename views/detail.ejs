    <div class="container mt-5">
      <div class="row">
        <div class="col-md-8">
        <% if(!contact){ %>
        <div class="alert alert-danger fw-bold" role="alert">
  kontak kosong <br> <a href="/contact" class="alert-link fw-normal">&laquo; kembali ke halamn contact</a>
</div>
        <% } else{ %>
          <div class="card" style="width: 18rem;">
          <div class="card-body">
           <h5 class="card-title"><%= contact.nama%></h5>
           <h6 class="card-subtitle mb-2 text-body-secondary"><%= contact.noHP%></h6>
           <p class="card-text"><%= contact.email%>.</p>
           <!-- <a href="/contact/delete/<%= contact.nama %>" class="btn btn-danger badge" onclick="return confirm('yakin')">Hapus</a> -->
           <form action="/contact?_method=DELETE" method="POST" class="d-inline" onclick="return confirm('yakin?')">
            <input type="hidden" name="nama", value="<%= contact.nama %>">
            <button type="submit" class="btn btn-danger badge">Hapus</button>
           </form>
           <a href="/contact/edit/<%= contact.nama %>" class="btn btn-info badge">ubah</a>
          <a href="/contact" class="card-link d-block mt-3">&laquo; kembali ke contact</a>
  </div>
</div>
        </div>
      </div>
    </div>  
<% } %>